# D3 价格监控 — 交接文档 (2026-06-26)

> 给下一个接手的 Claude / 开发者。**先读这个,再动代码。**
> WSL 分支 `telegram-ai-chat-mode` · HEAD `2c25ae4`
> 相关 memory(已最新,按序读):`d3-current-state` → `d3-price-after-voucher-fix`(最权威) → `d3-architecture` → `d3-runbook`。

---

## 一、这轮已完成并上线 — 别重做

| 内容 | commit | 说明 |
|---|---|---|
| After-voucher 红价直读 | `f49fd82` | scraper 用 `product_price.final_price_info` 读显示模型精确红价,不再自算 10% 文字券 |
| lib-records 透传字段 | `44960a8` | `normalizeVariant` 透传 `displayPrice`/`voucherSource`(旧版丢字段→worker fallback raw)。dashboard 一直显示 raw RM553.90 的真因 |
| Product 不存在→Not Selling | `6eea718` | scraper 检测 "The product doesn't exist"→`notExist:true`→runOnce `buildNotSellingRecord`→D1 清旧价,不 fallback 旧价 |
| dashboard "Product Removed" | `0a88a76` | 三态:Product Removed(链接没了)/ Not Available(在但不可卖/灰)/ Not Selling(无记录) |
| 价格 source 标签 | `2c25ae4` | dashboard 标 exact/est/raw,**只 exact 参与 gap / 最低价 / cheaper** |

**source 含义**:
- `exact` = `final_price_info(exact)`,即 display_model 那个变体的真红价 → 可信,参与比较,不标。
- `estimated` = 同券套 raw 估算 → 标橙色 `est`,显示但**不参与**比较。
- `raw` = 原价/无券/旧 fallback → 标灰色 `raw`,显示但**不参与**比较。
- take back 不受影响(来自 Google Sheet 独立值,不用竞品价算)。

**当前 D1 source 分布**:`exact:25 / estimated:845 / raw:1041 / removed:0`。
→ 大部分 model 行 gap 现在是「—」,**有意保守**(宁可少比也不拿估算价误判谁便宜)。Leon 已知情并接受。

---

## 二、待办 — 别擅自开始,等 Leon 点头

1. **遗留验证**:"Product Removed" 只测了 worker 层(POST 模拟 product_not_exist→D1 清空),**没用真实已下架 URL 跑过整条链路**。
2. **下一步大工程 — 逐变体 DOM 点选 `exact_selected`**:现在只有显示模型变体是 exact,其余 estimated/raw。要让所有显示变体精确(25→全覆盖、gap 恢复),得真去点每个 Package+Model 读 DOM 红价。反爬重、慢,**先设计抓取节奏再开**。Leon 明确推迟到单独一轮。

---

## 三、铁律 — 违反会出事

- **别整包 sync** runtime / C:\D3。三方仓库分叉:WSL `/home/dthree3/d3-price-monitor` / 运行态 `C:\Users\Asus\D3-runtime` / 同步源 `C:\D3`。任何 patch 必须以 `f49fd82` 为基准 **file-by-file surgical 合并**,否则会引入别人累积的接口券/anti-bot/intake 工作,或洗掉本轮 patch。
- 改 runtime 后**直接 `node` 重启**,别用 `start-hermes.ps1`(它先跑 `sync-runtime` 会清掉 patch)。
- dashboard 从 WSL deploy(Vercel),**不经 sync-runtime**,防覆盖靠 WSL commit。**别 patch `C:\D3\d3-price`**。
- worker 只部署 WSL 版 `/home/dthree3/d3-price-monitor/d3-worker`,**别用 `C:\D3\d3-worker`**。
- 这三个 .env 开关是 Leon 明确要求,**别改**:`HERMES_NOTIFY=0`(总静音 Telegram)、`HERMES_SELF_SWEEP_MINUTES=0`(关自家店高频)、只留 competitor 定时 `09:30/13:00/16:30`。
- Leon **拒绝 CAPTCHA / 反爬绕过**工具(见 memory `user-leon`)。
- 调价格 bug **别只看页面**,逐层查链路:scraper `[price]` log → `records.json` → D1 → worker 隔离测 → dashboard。

---

## 四、运行态

- Hermes idle,self-cycle 已关,只跑 competitor 定时 09:30/13:00/16:30。
- `HERMES_PRICE_DEBUG=1` 观察中(变体多日志大,验证完可设 0)。
- CDP 专用 Chrome 易挂,拉起命令见 memory `d3-price-after-voucher-fix` / `d3-runbook`。
