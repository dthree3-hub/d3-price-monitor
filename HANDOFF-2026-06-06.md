# D3 Handoff — 2026-06-06

这份是给 Claude 接手网页设计用的。重点不是“今天改了什么文件”，而是：

1. 现在哪条数据链已经定下来了
2. 哪些逻辑不要再动
3. Claude 只该接手哪些部分

## 当前已经完成的东西

### 1. Hermes 抓取链已经改成结构化数据
- 抓取端不再只存原始 `variant.name`
- 现在每个 variant 会额外存：
  - `model`
  - `color`
  - `capacity`
  - `tier`
  - `confidence`
- 核心文件：
  - `src/variant-parser.mjs`
  - `src/lib-records.mjs`
  - `src/runOnce.mjs`

### 2. 混卖链接现在按款式拆，不再按整条标题乱挂价格
- 之前像 `A56 / A36 / A26 / A17 / A07 / A06` 这种混卖标题会导致网页误把整条 listing 当成一个型号
- 现在规则改成：
  - 优先按 `variant.name` 识别 `model`
  - 再拆 `color / capacity / tier`
  - 网页只显示 Hermes 已经拆好的结果
- 例如已处理：
  - `A56256GBOlive` → `A56 / Olive / 256GB`
  - `A175G256GBBlack` → `A17 5G / Black / 256GB`
  - `12+512GB, Violet,Promo` → `S26+ / Violet / 12+512GB / Promo`

### 3. 线上网页不再把旧快照当成最新数据展示
- 线上页现在以云端数据为主
- 已把最新 Windows runtime 数据回收到仓库并重新规范化
- 然后推送到云端，再部署到 Vercel
- 当前线上地址：
  - `https://d3-price.vercel.app`

### 4. 云端数据源已改成“只保留最新一段”
- Pantry 有 payload size 限制，整份历史会报 `413 Payload Too Large`
- 所以现在 Hermes 云端同步规则是：
  - 只上传最新 `120` 条 record
- 这样目的很明确：
  - 保证线上展示“最新”
  - 不再混旧数据
  - 保证同步稳定

### 5. 店名映射已经补齐
- 下拉筛选和表格不会再一半显示店名、一半显示 shopId
- 当前固定映射：
  - `54618012` → `Our Store`
  - `116917349` → `Deal Direct`
  - `77792787` → `Spray Gadget`
  - `271823454` → `TAC Mobiles`
  - `56447030` → `Urban Republic`

### 6. 下方明细表已经改成结构化列
- 不再只显示 `Variant`
- 现在显示：
  - `Color`
  - `Capacity`
- `tier` 作为小字显示在容量下面

### 7. 金额格式已经统一
- 页面金额统一保留两位小数
- 不再出现：
  - `RM29.8099999999`
  - `RM34.4400000000`

### 8. 券后价规则已经统一
- 当前规则是：
  - 自动抓到的商品价，网页统一按 `-RM5 voucher` 后显示和比较
- 这是为了和 Shopee 页面的 `After Voucher` 红字尽量一致
- 这套规则现在已经用于：
  - 老板视图
  - 最低价卡片
  - gap
  - 下方明细表
  - 掉价展示

## 当前不要再动的部分

下面这些现在属于“逻辑冻结区”，Claude 不要碰：

### A. Hermes 的结构化解析规则
- 不要再把型号识别逻辑搬回前端
- 不要让网页重新自己猜 `model`
- 现在的正确方向是：
  - Hermes 解析
  - 网页展示
- 不要反过来

相关文件：
- `src/variant-parser.mjs`
- `src/lib-records.mjs`
- `src/runOnce.mjs`

### B. 云端同步策略
- 不要再把整份历史直接推 Pantry
- 现在已确认会超限
- 当前保留最新 `120` 条是有意设计，不是临时凑合

### C. 店名映射规则
- 不要删掉 `shopId -> 店名` 的 fallback
- 否则又会回到一半显示名字、一半显示号码

### D. 券后价统一规则
- 目前页面已经统一按 `-RM5` 展示
- Claude 如果做视觉设计，不要再把有些区块改回未扣券价，有些区块保留扣券价
- 否则用户会再次看到“同一商品上下价格不一致”

## Claude 现在适合接手的范围

Claude 现在只适合做这些：

### 1. 纯网页视觉和布局
- 排版
- 留白
- 色彩
- 字体
- 卡片视觉
- 表格可读性
- 响应式
- 交互动效

### 2. 不改变数据逻辑的 UI 优化
- 把 `Shop / IDs` 视觉分层
- 把 `tier` 做成 badge
- 更好的筛选区布局
- 老板视图更清楚的 competitor 切换样式
- 下方明细表的信息层次优化

### 3. 文案优化
- 英文 UI 文案
- 标签命名
- 提示语

## Claude 暂时不要接手的范围

### 1. 不要改 Hermes / scraper / parser
- 不要改：
  - `src/variant-parser.mjs`
  - `src/runOnce.mjs`
  - `src/lib-records.mjs`
  - `src/scraper.mjs`

### 2. 不要改云端同步规则
- 不要改 Pantry payload 策略
- 不要把完整历史重新塞回去

### 3. 不要把旧快照逻辑加回来
- 不要重新让线上页优先显示 embedded snapshot / stale local cache
- 用户已经明确要求不要再看到旧数据

## 现在已知但还没彻底解决的事

这些是“已知边角”，不是主链未完成：

### 1. 某些卖家的 variant 命名仍然很脏
- 例如：
  - `LIMITED TIME ONLY`
  - `SKYBLUE`
  - `JETBLACK`
  - 某些 `Watch` 款式前缀
- 现在多数已经能拆
- 但如果后面再出现新的脏格式，需要继续加 parser 规则

### 2. `RM5 voucher` 规则是统一展示规则，不是实时检测
- 当前不是逐商品检测页面上“有没有券”
- 是按统一展示规则处理
- 这是为了稳定，不是为了 100% 还原每次会话中的临时促销状态

## 如果明天继续开发，正确顺序

1. 先保持 Hermes 数据链不动
2. 只做网页设计和信息层级优化
3. 如果发现具体某条商品解析不对，再单点补 parser 规则
4. 不要重新回到“前端猜型号、前端猜价格归属”

## 当前状态结论

可以把项目分成两块看：

- 数据逻辑：现在由 Hermes 主导，已定型，先不要再动
- 网页视觉：可以交给 Claude 继续做

这就是当前最稳的交接边界。
