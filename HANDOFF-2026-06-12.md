# HANDOFF 2026-06-12（给下一个 Claude）

本次会话改了三块：**worker 同步修复（已部署）**、**scraper 灰组合过滤（已回退）**、**前端 Dashboard 三处改动（已 push，未 deploy Vercel）**。

---

## 1. 任务 B：P2C orphan 永久修复 —— ✅ 已部署

- `d3-worker/src/index.js` `/api/sync`：每 item「先清后插」（`DELETE WHERE shop_id+item_id` + `INSERT` 当前变体，同一 `env.DB.batch()` 事务）。无有效变体的 item 不删；`price_history` 不动。
- commit `aa82b0b`（上一个 Claude 写的），本次 **deploy 成功**：Worker Version `b7f7553f-7dd1-4e95-baf3-339dac41ff90`。
- 实测：跑了一轮 D3 all 全店同步后，`variant_prices` 由 **2150 → 1748 行**（净删 ~402 行 orphan）。先清后插确实生效。

## 2. 任务 A：scraper 过滤灰掉的 package —— ❌ 已回退（方案不成立）

- 原方案（commit `42dee59`）：`src/scraper.mjs extractFromPdp` 跳过 `is_grayout===true || is_clickable===false` 的 model。
- **回退原因（实测验证）**：用 A06 listing（itemId `2040352813`，**这是自家店 shopId 54618012「Our Store」，不是 Spray Gadget**）单抓拿 raw 验证——
  - 75 个 model **全部** `is_grayout=false` / `is_clickable=true`，`grayedOutCount=0`，过滤删不掉任何东西。
  - CLAUDE.md 里「`is_grayout=true ⟺ is_clickable=false`」只在 **Tab A11** 上验证过，**对 A06 不成立**。
  - A06 的「不可买/占位」组合唯一的数据信号是 **RM999 dummy 价**（`price=99900000` + `promotion_price=null` + `price_before_discount=0` + `promotion_id=0`，共 6 个）。`promotion_id==0` 单独不够干净（6 个真组合也是 0）。
  - 注意：A06 5G 128GB Green 的 `Promo set`=RM561.81 是**真实可买促销**（有真 promotion_id），不一定该消失。
- **已回退** commit `45d8078`（revert 42dee59），WSL + `C:\D3` + `D3-runtime` 三处 scraper 均已回到无过滤。
- **待办（等 Leon 拍板）**：① 页面选 Green 后 Promo（561.81）到底灰不灰？② 过滤信号用 RM999 占位价 / 读 DOM 置灰态 / 暂不做。Leon 之前要求「靠页面 disabled 状态、不靠价格」，但 A06 的 PDP 没有 disabled 信号 → 这条原则和现实冲突，需重新确认。

## 3. 前端 Dashboard：`d3-price/index.html` 三处改动 —— ✅ 已 push，⚠️ 未 deploy

> Leon 看着实际页面要求的。git push **不会**触发 Vercel，需手动 `cd d3-price && npx vercel --prod --yes` 才会上线。

1. **移除 Urban Republic 商家**：`activeRoles` 由 `['A','B','C','D']` → `['A','B','C']`（line ~2771）。对比表 Urban Republic 列 + 竞品按钮都消失。另删了 watchlist 下拉的 `Urban Republic` option、更新了说明文字。内部角色映射（`D:'Urban Republic'`、shopId `56447030`）保留但不渲染。
2. **删除「Price drop report」版块**：移除 `<section class="report-wrap">` 整段 HTML；对应 JS 改 null-safe（`if (metricDrops)`、`renderDropList` 开头 `if (!dropList) return`），避免元素不存在时报错。
3. **「Captured product details」默认空表**：`render()` 里加 `hasActiveFilter` 判断——只有 `state.search || shopFilter || seriesFilter || modelFilter || activeBossRole()` 任一激活，才列出对应款式；否则显示提示文字、不渲染 100+ 行。

---

## 硬约束 / 环境（沿用 CLAUDE.md）
- Worker 只部署 WSL 仓库 `/home/dthree3/d3-price-monitor/d3-worker`，不要用 `C:\D3\d3-worker`。
- 前端部署：`cd d3-price && npx vercel --prod --yes`（必须手动）。
- WSL 看不到 Windows CDP（localhost 不互通）；抓取/CDP 操作要在 Windows 端（`powershell.exe` / `C:\D3`），抓取会驱动生产 Chrome，动手前先跟 Leon 确认。
- Hermes scraper 跑在 Windows：`C:\D3\src`（D3 all 全扫）/ `D3-runtime`（定时 Hermes，由 `sync-runtime.ps1` 镜像）。改了 WSL 代码要 `git push` → `C:\D3 git pull` → `sync-runtime.ps1` 才到生产。
